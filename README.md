# rap-employee-leave-management
This project demonstrates a Managed RAP (RESTful ABAP Programming Model) application built on **SAP BTP ABAP Environment (Trial)**.  The application manages employee leave requests and exposes data using **OData V4** with a **Fiori Elements List Report** preview.
